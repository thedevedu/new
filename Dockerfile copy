FROM python:3.9.7

WORKDIR /home/

RUN git clone https://github.com/thedevedu/new.git

WORKDIR /home/new/

RUN pip install -r requirements.txt

RUN pip install gunicorn

RUN echo "SECRET_KEY=django-insecure-d53-cb(9w+k)!el9-bfno$e72=v6t*5&&thm&%z@iq%r@%fvqo" > .env

RUN python manage.py migrate

EXPOSE 8000

CMD ["gunicorn", "gift.wsgi", "--bind", "0.0.0.0:8000"]
